export type SavedOpportunity = {
  id: string;
  title: string;
  organization: string;
  estimatedValue: string;
  location: string;
  savedAt: string;
  alertDate: string | null;
  alertDone: boolean;
};
